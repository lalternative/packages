# @lalternative packages are published to the public npmjs.org registry.
# No scoped registry override and no token needed — install is anonymous.
# (publish target is set per-package via publishConfig in each package.json)
